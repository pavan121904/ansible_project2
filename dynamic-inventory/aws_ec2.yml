plugin: amazon.aws.aws_ec2
regions:
  - eu-north-1
keyed_groups:
  - key: tags.Enviro
    prefix: environment
    separator: ''
    filter: tag:Environment=dev
  - key: tags.Environment
    prefix: environment
    separator: ''
    filter: tag:Environment=testing

